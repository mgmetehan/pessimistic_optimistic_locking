spring:
  application:
    name: pessimistic_optimistic_locking
  datasource:
    url: jdbc:postgresql://localhost:5432/test_db
    username: postgres
    password: toor
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: 'true'
    hibernate:
      ddl-auto: update
